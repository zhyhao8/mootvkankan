# 宝塔面板安装教程（Linux）

> 适用对象：刚接触服务器运维、想快速搭建网站环境的用户。  
> 适用系统：CentOS / Ubuntu / Debian（纯净系统推荐）。

---

## 1. 安装前准备

1. 一台云服务器（建议 2 核 2G 起步）。
2. 服务器已获取 `root` 权限。
3. 已放行常见端口（安全组 / 防火墙）：
   - `22`（SSH）
   - `80`（HTTP）
   - `443`（HTTPS）
   - 宝塔面板端口（安装完成后会给出，如 `8888`）。
4. 先更新系统：

```bash
# CentOS
yum update -y

# Ubuntu / Debian
apt update -y && apt upgrade -y
```

---

## 2. 一键安装宝塔

> 请在 **官方文档** 获取最新安装命令，避免使用来路不明脚本。

### 2.1 CentOS 安装（示例）

```bash
yum install -y wget && wget -O install_panel.sh https://download.bt.cn/install/install_panel.sh && bash install_panel.sh ed8484bec
```

### 2.2 Ubuntu / Debian 安装（示例）

```bash
wget -O install_panel.sh https://download.bt.cn/install/install_panel.sh && bash install_panel.sh ed8484bec
```

安装过程通常需要 2~10 分钟，结束后终端会输出：
- 面板访问地址（URL）
- 初始用户名
- 初始密码

请立即保存这些信息。

---

## 3. 首次登录与基础配置

1. 浏览器访问安装输出的面板地址（例如：`http://你的IP:8888/xxxxx`）。
2. 输入初始账号密码登录。
3. 绑定宝塔账号（可选，但建议绑定以便插件管理）。
4. 按提示安装推荐环境（LNMP 或 LAMP）。

建议最小安装：
- Nginx
- MySQL（或 MariaDB）
- PHP（按项目版本选）
- phpMyAdmin（可选）

---

## 4. 服务器安全加固（强烈建议）

### 4.1 修改面板默认配置
- 修改面板默认端口（避免常见端口扫描）。
- 修改面板入口路径（如果版本支持）。
- 立即修改初始管理员密码为高强度密码。

### 4.2 开启登录保护
- 开启面板登录失败限制。
- 启用两步验证（2FA，如有）。
- 只允许固定 IP 登录面板（白名单）。

### 4.3 系统防火墙
根据系统启用防火墙，仅放行需要端口：

```bash
# Ubuntu / Debian (ufw)
ufw allow 22
ufw allow 80
ufw allow 443
ufw allow 8888   # 替换成你的宝塔端口
ufw enable
```

---

## 5. 部署网站（通用）

1. 进入【网站】→【添加站点】。
2. 填写域名，选择根目录。
3. 选择是否创建数据库（按需）。
4. 上传网站文件到站点目录。
5. 在【SSL】中申请证书（Let’s Encrypt），并开启强制 HTTPS。
6. 在【伪静态】选择对应框架规则（如 Laravel、WordPress、ThinkPHP 等）。

---

## 6. 常见问题排查

### Q1：无法访问面板
- 检查云安全组是否开放面板端口。
- 检查防火墙是否放行端口。
- 使用以下命令确认端口监听：

```bash
ss -lntp | rg '8888|bt|panel'
```

### Q2：面板忘记密码
在 SSH 执行宝塔官方提供的重置命令（不同版本命令略有差异），建议去官方文档查询最新命令。

### Q3：申请 SSL 失败
- 域名未解析到当前服务器。
- 80/443 端口未开放。
- DNS 还未生效（需等待）。

---

## 7. 维护建议

- 每周至少一次系统与面板更新。
- 定期备份网站文件 + 数据库（异地备份）。
- 不使用弱口令，避免多个服务器共用同一密码。
- 定期检查登录日志与异常进程。

---

## 8. 快速命令清单

```bash
# 查看宝塔相关服务状态（示例）
bt

# 查看监听端口
ss -lntp

# 查看磁盘空间
df -h

# 查看内存使用
free -m
```

如果你愿意，我还可以继续给你补一份 **“宝塔上部署 MooTV（静态站点）”** 的一步一步图文版（包含 Nginx 配置与 HTTPS）。
